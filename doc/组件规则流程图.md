# 组件规则流程图

> 本文档是 `.cursor/rules/project-conventions.mdc` 的流程可视化。修改规则后须同步更新本文档。

## 0. 元流程：Cursor Feedback

```mermaid
flowchart LR
    A[任意任务] --> B[执行工作]
    B --> C[调用 cursor-feedback 汇报]
    C --> D{用户反馈?}
    D -->|有| B
    D -->|结束| E[完成]
```

---

## 1. 任务入口分流

### 方案 A（旧，已废弃）

```
不涉及 component/ → 笼统走「全局规范：路径/风格/三端/npm」
```

问题：三端/风格应在**写组件 UI 时**才需要，不应挂在「不涉及 component」上。

### 方案 B（现行）

```mermaid
flowchart TD
    Start[任务入口] --> T{任务类型?}

    T -->|新增顶层 component 组件| N1["§2 分类确认"]
    T -->|修改已有顶层 component| M1["§3 创建/修改代码（跳过分类询问）"]
    T -->|CRUD 内部子组件 modes/*| I1["§4 内部子组件路径"]
    T -->|改 util/| U1["路径 + 命名 + util 导出"]
    T -->|改 config/| C1["路径 + 配置类型"]
    T -->|仅改 Demo 文档| D1["§5 Demo 规范"]
    T -->|改 Cursor 规则| R1["§6 规则维护"]
    T -->|其他| O1["按需：路径/命名/代码风格"]

    N1 --> N2["§3 创建代码"]
    N2 --> N3{是否对外发布?}
    N3 -->|是，须有 Demo| N4["§5 Demo + §3 导出注册"]
    N3 -->|否| N5[不导出、不写菜单]
    N4 --> N6[检查清单]

    M1 --> M2{Props/Events 变更?}
    M2 -->|是| M3["§5 同步 Demo"]
    M2 -->|否| M4[检查清单]
    M3 --> M4

    I1 --> I2[父目录内实现，不导出/Demo菜单]

    R1 --> R2["更新 project-conventions.mdc"]
    R2 --> R3["同步本文档"]
```

---

## 2. 分类确认（仅新增顶层组件）

当前 `component/` 子目录以 `project-conventions.mdc`「组件分类（全局）」表格为准：`basic/`、`crud/`、`interaction/`。

> **说明**：不存在 AI 运行时读取的独立子目录配置文件；新增目录后须**回写** `project-conventions.mdc` 表格，规则才生效。

```mermaid
flowchart TD
    Start[新增顶层组件] --> Q1{需求是否已明确指定目录?}
    Q1 -->|否| Ask1["询问：basic / crud / interaction ?"]
    Q1 -->|是| Fit{是否 fit 分类表?}
    Ask1 --> Fit

    Fit -->|能归入某一类| OK[进入 §3 创建代码]
    Fit -->|都不适合| Pause["暂停：询问是否新增 component/ 子目录"]

    Pause --> Ask2["须问清：是否新增 / 目录名 / 存什么类型"]
    Ask2 --> Approve{开发者确认?}
    Approve -->|否| Stop[停止或重选现有目录]
    Approve -->|是| Update[回写规则与工程文件]

    Update --> U1["project-conventions 全局分类表"]
    Update --> U2["脚手架第一节分类表"]
    Update --> U3["src/type/{category}.ts + type/index.ts"]
    Update --> U4["component/index.ts export 区块"]
    Update --> U5["src/index.ts 导出 + components 数组"]
    Update --> U6["App.vue 新 el-sub-menu（如需）"]
    Update --> U7["doc/组件规则流程图.md"]

    U7 --> OK
```

`doc/架构设计.md`、`CHANGELOG.md` **不在此流程自动更新**，仅开发者明确要求时维护。

---

## 3. 创建 / 修改代码

```mermaid
flowchart TD
    A[编写或修改组件] --> B[路径引用 @/ alias]
    B --> C[script setup + Composition API]
    C --> D[类型写入 src/type/{category}.ts]
    D --> E[SCSS 使用 variables.scss $lib-*]
    E --> F[三端断点 tablet / mobile]
    F --> G{能否独立给业务方使用?}
    G -->|能，且 basic 无同类| H["询问是否抽离到 basic/"]
    G -->|不能| I[继续]
    H --> I
```

---

## 4. 大业务组件内部件 vs 可抽离小组件

**判断标准**：该 Vue 文件能否作为**通用小组件**单独给业务方使用？

```mermaid
flowchart TD
    A[在大业务组件内新增 .vue] --> B{能否独立给业务方使用?}
    B -->|能| C{basic/ 已有同类?}
    C -->|有| D[复用 basic 已有组件]
    C -->|无| E["询问开发者：是否抽离到 basic/ 并导出"]
    E -->|确认抽离| F["basic/ 新组件 + Demo + 导出"]
    E -->|不抽离| G[留在父组件内，不导出]

    B -->|不能，仅服务当前大组件| H["留在 modes/ 等子目录"]
    H --> I[不导出、不写 Demo 菜单]

    J[对外发布] --> K["只导出门面，如 BaseTable"]
```

示例：`BaseTableElement` 等五种模式 **不能** 独立使用 → 留 `modes/`，不导出。若某 Cell 组件 **可以** 单独复用 → 询问是否进 `basic/`。

---

## 5. Demo 同步 + 导出注册

```mermaid
flowchart TD
    A[对外发布的顶层组件] --> B{有 Demo?}
    B -->|否| Stop["停止：不得导出、不写菜单"]
    B -->|是| C["Demo{Component}.vue"]
    C --> D[demoCodes.ts 完整 script+template]
    D --> E{opaque 类型?}
    E -->|是| F[apiTypeHints.ts]
    E -->|否| G[App.vue pageMap + menu]
    F --> G
    G --> H[ApiTable + DemoWidgetTabs]
    H --> I[component/index.ts 导出]
    I --> J["src/index.ts 导出 + components 数组"]
```

---

## 6. Cursor 规则维护

```mermaid
flowchart TD
    A["修改 project-conventions.mdc"] --> B["同步 doc/组件规则流程图.md"]
    A --> C{开发者要求?}
    C -->|更新架构文档| D["doc/架构设计.md"]
    C -->|记录变更| E["CHANGELOG.md"]
```

---

## 硬约束速查

| 场景 | 行为 |
|------|------|
| 新增顶层组件，分类不明 | 必须询问，不得默认 `interaction/` |
| 修改已有顶层组件 | 跳过分类询问 |
| 三者均不适合 | 暂停，询问是否新增目录 |
| 新增目录后 | 回写规则表 + type/index/component/index/src/index/流程图 |
| CRUD 内部件 | 不能独立使用 → 父目录内，不导出 |
| 大业务组件 | 只导出门面（如 BaseTable） |
| 可独立小组件 | basic 无同类 → 询问是否抽离到 basic |
| 无 Demo | 不得对外发布 |
| 架构/CHANGELOG | 仅开发者明确要求时更新 |
| 改 Cursor 规则 | 同步本文档 |
