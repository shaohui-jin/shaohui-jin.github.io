# 项目架构设计

## 一、项目定位

**comp-vue-lib** 是一个基于 **Vue 3 + TypeScript + Vite** 的企业级 UI 组件库，核心亮点是 **BaseTable 五种渲染模式**（从 DOM 到 Canvas/Skia WASM），适配从小数据到超大数据集的表格场景。

---

## 二、技术栈

| 层面 | 技术 |
|------|------|
| 框架 | Vue 3.5 + Composition API (`<script setup>`) |
| 类型 | TypeScript 5.6 |
| 构建 | Vite 5（开发 + 库模式打包） |
| UI 基座 | Element Plus 2.8 |
| 高性能渲染 | Canvas 2D / CanvasKit（Skia WASM） |
| 包管理 | pnpm 10 |
| 代码质量 | ESLint + Prettier + Stylelint |
| 类型声明 | vite-plugin-dts（rollupTypes 聚合输出） |

---

## 三、目录结构

```
comp/
├── src/                        # 组件库源码（打包入口）
│   ├── index.ts                # 库入口：install() + 具名导出
│   └── components/
│       ├── index.ts            # 组件桶导出
│       ├── crud/               # CRUD 业务组件
│       │   ├── base-table/     # ⭐ 核心：五模式表格
│       │   │   ├── BaseTable.vue       # 外壳/路由层
│       │   │   ├── types.ts            # Props/Emits/Column 类型
│       │   │   ├── theme/              # 主题 token + CSS 变量
│       │   │   ├── modes/              # 五种渲染实现
│       │   │   └── utils/              # 组合式函数与工具
│       │   ├── base-search/            # 配置式搜索栏
│       │   ├── base-search-drawer/     # 抽屉式高级搜索
│       │   └── base-column-setting/    # 列显隐/排序设置
│       ├── hello-button/       # 基础按钮
│       ├── search-bar/         # 简易搜索条
│       ├── status-tag/         # 状态标签
│       ├── status-dot/         # 状态圆点
│       ├── text-link/          # 文本链接
│       └── empty-placeholder/  # 空状态占位
├── examples/                   # 演示站点（开发调试）
│   ├── App.vue                 # 导航 + 动态路由
│   ├── demos/                  # 各组件 Demo
│   └── index.html
├── vite.config.ts              # 开发服务器配置
├── vite.lib.config.ts          # 库模式打包配置
└── package.json
```

---

## 四、核心架构：BaseTable 五模式

BaseTable 是本项目的核心组件，通过 `mode` prop 切换五种渲染策略：

```
BaseTable.vue（外壳）
  ├── BaseTableElement.vue      → el-table 封装（功能最全）
  ├── BaseTableVirtual.vue      → el-table-v2 虚拟滚动
  ├── BaseTableCanvas.vue       → Canvas 2D 视口重绘
  ├── BaseTableCanvasTile.vue   → Canvas 整表预渲染 + 裁切
  └── BaseTableSkiaWasm.vue     → CanvasKit GPU 加速渲染
```

### 设计思路

1. **统一接口**：外壳 `BaseTable.vue` 接收统一的 `columns` / `tableData` / `rowKey` 等 props，内部 `<KeepAlive>` 按 mode 分发
2. **统一主题**：`tableSurfaceTokens` 定义颜色/字号/行高等 token，DOM 模式通过 CSS 变量注入，Canvas 模式读取 JS 常量绘制
3. **渐进增强**：element 模式能力最全（支持 slot、排序、展开行等），canvas 系模式逐步覆盖交互（checkbox hover、tooltip、滚动条等）
4. **可选依赖**：`canvaskit-wasm` 标记为 `peerDependenciesMeta.optional`，仅 skia-wasm 模式需要

### 各模式适用场景

| 模式 | 适用场景 | 特点 |
|------|----------|------|
| `element` | 常规数据量（<1000 行） | 能力最全，支持所有 Element Plus 特性 |
| `virtual` | 万级行数据 | DOM 仍可控，虚拟滚动 |
| `canvas` | 超大数据、结构简单 | 单 Canvas，视口内重绘 |
| `canvas-tile` | 超大数据 | 整表预渲染，滚动裁切视口 |
| `skia-wasm` | GPU 加速需求 | CanvasKit 渲染，需加载 wasm |

### 共享工具层

- `canvasDraw.ts` — Canvas 2D 通用绘制函数
- `useCanvasScrollbar.ts` — 自定义滚动条
- `useCanvasTooltip.ts` — hover 显示完整文本
- `useCanvasCheckboxHover.ts` — checkbox 交互
- `useCanvasSlotPopup.ts` — Canvas 模式弹窗
- `useBaseTableSelection.ts` — 行选择逻辑
- `column.ts` — 列宽计算（五模式对齐）
- `tableHitTest.ts` — Canvas 点击命中检测
- `selectionKeys.ts` — 选择键管理
- `skiaTypeface.ts` — Skia 字体加载
- `canvaskitLocate.ts` — CanvasKit WASM 资源定位

---

## 五、主题系统

```
tableSurfaceTokens (JS 常量)
    ↓
┌────────────────────────────────────────┐
│ DOM 模式: tableSurfaceCssVars()        │ → CSS 变量注入到 .crud-base-table
│ Canvas 模式: 直接读取 token 绘制       │
└────────────────────────────────────────┘
```

同时覆盖 Element Plus 的 `--el-table-*` 变量，确保 element 模式视觉一致。

### 主题 Token 包含

- 表面背景色 / 表头背景
- 边框线颜色
- 正文 / 表头 / 空状态文字色
- 斑马纹行 / hover 行背景
- Checkbox 边框 / 选中色
- 字体族 / 字号 / 字重
- 行高 / 列宽默认值

---

## 六、构建与分发

| 配置 | 用途 |
|------|------|
| `vite.config.ts` | 开发服务器，root 设为 `examples/`，热更新开发 |
| `vite.lib.config.ts` | 库模式打包，输出 ESM + CJS + 类型声明 + sourcemap |

### 打包策略

- **外部化**：vue / element-plus / lodash-es / canvaskit-wasm 不打入包内
- **资产复制**：自定义 Vite 插件将 `canvaskit.wasm` 和字体文件复制到 dist/assets
- **双格式**：`index.mjs`（ESM）+ `index.js`（CJS）
- **类型聚合**：`vite-plugin-dts` 配合 `rollupTypes: true` 输出单一 `.d.ts`

### 导出接口

```ts
// 插件式全量注册
import CompVueLib from 'comp-vue-lib'
app.use(CompVueLib)

// 按需具名导入
import { BaseTable, BaseSearch } from 'comp-vue-lib'

// 样式
import 'comp-vue-lib/style.css'
```

---

## 七、三端适配策略

通过 SCSS 断点变量统一管理：

| 端 | 断点 | 变量 |
|----|------|------|
| PC 端 | > 1024px | 默认布局 |
| 平板端 | 768px ~ 1024px | `$doc-bp-tablet` |
| 手机端 | < 768px | `$doc-bp-mobile` |

### 适配要点

- 交互元素点击区域不小于 32×32px
- 布局在窄屏下不溢出
- Canvas 模式支持触摸滚动（惯性 + 方向判断）
- 抽屉组件移动端全屏适配
- 滚动穿透锁定

---

## 八、组件设计模式

1. **类型先行**：每个组件有独立 `types.ts`，定义 Props / Emits 接口
2. **配置驱动**：BaseSearch / BaseTable 通过 JSON 配置数组描述字段/列
3. **组合式函数**：复杂逻辑抽取为 `use*` hooks（如 `useCanvasScrollbar`）
4. **插件式安装**：导出 `install()` 支持 `app.use()`，也支持按需具名导入
5. **类型导出**：库入口统一导出所有公共类型，消费方无需深层引用

---

## 九、演示站点

- 入口：`examples/App.vue`
- 布局：flex tree 结构（header 固定 + 主区域 flex-1 + 左导航固宽 + 右内容自适应）
- 导航：Element Plus `el-menu` 侧边栏，分 CRUD 组件 / 基础组件两组
- 样式变量：`examples/demos/variables.scss` 统一管理演示站设计 token
- 通用组件：`ApiTable.vue` 响应式 API 文档表格（桌面端完整表格 / 移动端精简展开）
- 特色：内置 `ChangelogPanel` 组件读取 CHANGELOG.md 展示版本历史
- 支持三端自适应，移动端 API 表格自动切换为精简展开模式

---

## 十、版本演进概览

| 版本 | 日期 | 里程碑 |
|------|------|--------|
| v0.1.0 | 2026-04-16 | 项目初始化 + BaseTable 五模式 |
| v0.2.0 | 2026-04-24 | Canvas 滚动优化 + Skia 增强 |
| v0.3.0 | 2026-05-03 | 架构重构 + 三端适配 |
| v0.4.0 | 2026-05-10 | 列宽统一 + ChangelogPanel |
| v0.5.0 | 2026-05-15 | BaseSearch / SearchDrawer / ColumnSetting |
| v0.6.0 | 2026-05-21 | ApiTable 组件 + 演示站 flex tree 布局重构 |
