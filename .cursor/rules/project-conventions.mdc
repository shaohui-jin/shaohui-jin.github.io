---
description: 项目级通用约定，每次对话自动生效
alwaysApply: true
---

# 项目约定

# Cursor Feedback 规则

1. 在任何流程、任务、对话进行时，无论是询问、回复、或完成阶段性任务，皆必须调用 MCP cursor-feedback。
2. 每当收到用户反馈，必须再次调用 MCP cursor-feedback，并根据反馈内容调整行为。
3. 完成任务前，必须使用 MCP cursor-feedback 工具向用户询问反馈。
4. 所有需要展示给用户的内容必须全部通过 cursor-feedback 的 summary 参数传递。

## 语言

始终使用简体中文回复。代码注释也优先使用中文，除非是公共 API 的 JSDoc。

## 目录结构规范

Monorepo 根目录使用 **pnpm workspace + turbo** 管理，源码按职责拆分为 workspace 包：

```
packages/
  core/                ← jsh-core：配置注入系统 + SCSS 变量 + 颜色 token
    src/config/
    src/style/
    src/index.ts
  comp/                ← jsh-comp：Vue 组件 + 类型声明
    src/style/         ← SCSS 转发层（@forward jsh-core 变量）
    src/component/
      basic/           ← 基础组件（Tag、Dot、图片类、CodeBlock 等）
      crud/            ← CRUD 组件（BaseTable、BaseSearch 等）
      interaction/     ← 高交互组件（SplitPane、DragSortList 等）
    src/type/          ← 类型声明（所有组件类型的唯一源头）
    src/index.ts       ← 组件库主入口（re-export jsh-core 配置 API）
  tool/                ← jsh-tool：工具函数
    src/
  rule/                ← jsh-rule：规则画布（Workflow Designer）
    src/
  blog/                ← jsh-blog：Markdown 内容路由
    src/
  playground/          ← 演示壳应用（路由聚合 + 文档渲染）
    src/
pnpm-workspace.yaml
turbo.json
package.json           ← 根编排（private）
```

`packages/playground/` 下按职责分离：

```
packages/playground/
  App.vue              ← 演示站入口
  style/               ← 演示站 SCSS（variables、demo、el-overrides 等）
  route/               ← 顶层 Tab / 分组 / 页面聚合
  doc-render/          ← Markdown 页面渲染
```

## 组件分类（全局）

`packages/comp/src/component/` 下当前为以下三个子目录：

| 分类 | 目录 | 适用场景 | Demo 菜单分组 |
|------|------|----------|--------------|
| **基础** | `basic/` | 简易 UI、状态展示、图片/文字/Canvas 展示、文档辅助（CodeBlock、WidgetTabs） | 「基础组件」「视觉组件」 |
| **CRUD** | `crud/` | 搜索、表格、列设置、CRUD 联动等表单/列表业务 | 「CRUD 组件」 |
| **交互** | `interaction/` | 非基础、高交互 UI（拖拽、穿梭、向导、右键菜单、动画数字、热力图、灯箱等） | 「交互组件」 |

**硬约束：**

- 新增组件前**必须先向开发者确认**属于哪一类；分类不明确时必须询问，不得自行推断或默认归入 `interaction/`
- 若判断组件**不适合**上文表格所列 `component/` 子目录中的任何一个，**必须暂停并询问开发者**：是否新增 `component/` 子目录、目录名称、以及该目录用于存放什么类型的组件；**未经确认不得自行创建新目录**
- **经开发者确认新增 `component/` 子目录后**，须同步更新：
  1. 本节「组件分类（全局）」表格
  2. 「组件一站式创建规范」第一节分类表
  3. 新建 `packages/comp/src/type/{category}.ts`（类型声明），并在 `packages/comp/src/type/index.ts` 追加 re-export
  4. `packages/comp/src/component/index.ts` 增加该分类的 export 注释区块
  5. `packages/comp/src/index.ts` 追加具名导出、`export type` 及 `components` 数组注册（该分类首个对外组件时）
  6. 若新目录对应新的 Demo 菜单分组，更新 `packages/comp/src/doc/CompDocRegistrar.ts`（分组）与 `packages/playground/App.vue`（展示）
  7. `doc/组件规则流程图.md` 同步更新流程图
  - 此后硬约束以上述更新后的目录列表为准
  - `doc/架构设计.md`、`CHANGELOG.md` **不在此流程自动更新**，仅当开发者明确要求记录文档/变更时再维护
- CRUD 父组件的内部子组件（如 `base-table/modes/CellSwitch.vue`）默认放在父组件子目录，**不单独导出**
- 若某实现**可以**作为通用小组件单独使用：先查 `basic/` 是否已有同类；**没有则询问开发者**是否抽离到 `basic/` 并对外导出
- 若**不能**独立使用（仅服务当前大业务组件）：留在父组件内（如 `base-table/modes/*`），**不导出、不写 Demo 菜单**
- 大业务组件**只对外导出业务门面**（如 `BaseTable`），内部多种实现不单独导出
- 无 Demo 的组件不得对外发布（不导出、不写菜单）

## 路径引用规范

- **包内**（`packages/comp`、`packages/core`）：使用 `@/` alias（`@` 指向该包 `src/`）
- **跨包**：使用 workspace 包名 `jsh-comp`、`jsh-core`、`jsh-tool`、`jsh-rule`、`jsh-blog`（禁止 `../../../`）
- 同目录内用 `./` 相对引用
- SCSS：`packages/comp` 内 `@use "@/style/variables"`；`packages/playground` 通过 sass `loadPaths` 继承 `jsh-core` 变量
- `packages/playground` 引用组件库使用 `jsh-comp` / `jsh-core` / `jsh-tool` / `jsh-rule` / `jsh-blog`
- 唯一例外：`node_modules` 运行时资源引用和项目根文件（如 `CHANGELOG.md`）

## 路由注册规范

- 需要在 `packages/playground` 展示页面的模块，必须通过继承 `jsh-core` 的 `BaseRouteRegistrar` 提供注册器
- 模块对外统一导出 `*RouteRegistrar`，壳应用只做聚合（`packages/playground/route/moduleRegistrars.ts`），不复制业务模块的路由定义
- `RouteRegistry` 注册阶段会校验注册器是否为 `BaseRouteRegistrar` 实例，非基类实例不予注册

## 代码风格

- Vue 组件使用 `<script setup lang="ts">` + Composition API
- 组件文件名：PascalCase（如 `ChangelogPanel.vue`）
- 组合式函数文件名：camelCase，`use` 前缀（如 `useCanvasCheckboxHover.ts`）
- 工具函数文件名：camelCase（如 `canvasDraw.ts`）
- SCSS 变量统一定义在 `packages/core/src/style/variables.scss`，Demo 页通过 `@use` 继承
- 主题 token 统一定义在 `packages/comp/src/component/crud/base-table/theme/tableSurface.ts`
- 类型声明统一放在 `packages/comp/src/type/`，组件目录内不再保留 `types.ts`
- 不要自行添加 npm 依赖，除非明确要求

## 组件复用原则

- 相同功能/UI 不允许重复实现，必须提取为通用组件复用
- 代码展示 + 复制功能统一使用组件库的 `CodeBlock` 组件，不要自行实现复制逻辑
- 预览/代码切换容器统一使用组件库的 `WidgetTabs` 组件（默认 `flush: true`）
- `packages/comp/src/doc/demo/DemoCodeBlock.vue` 和 `DemoWidgetTabs.vue` 是 Demo 站的薄包装层
- 新增 Demo 辅助组件前，先检查 `packages/comp/src/component/basic/` 下是否已有可复用的组件

## npm 包导出规范

组件、配置、工具函数使用不同 workspace 包：

```ts
// 组件 + 配置（主入口）
import { BaseTable, CodeBlock, ConfigProvider, createCompLib } from "jsh-comp";

// 工具函数（独立包）
import { rgbaToHex, debounce } from "jsh-tool";
import { rgbaToHex } from "jsh-tool/color";

// 类型（jsh-comp 子路径）
import type { TagProps, BaseTableColumn } from "jsh-comp/type";

// 配置底层 API（高级场景）
import { useLibConfig, defaultLibConfig } from "jsh-core";

// 样式
import "jsh-comp/style.css";
```

- `packages/comp/src/index.ts` 导出组件，并 re-export `jsh-core` 配置 API
- 工具函数通过 `packages/tool/src/index.ts` 统一导出（`jsh-tool`）
- 类型通过 `packages/comp/src/type/index.ts` 统一导出（`jsh-comp/type`）
- 各包 `package.json` 的 `exports` 字段维护子路径映射
- 构建：`turbo build`；`jsh-comp` 使用 `vite.lib.config.ts`（多入口）

## 文件命名

- 新增或修改文件时，文件名必须语义化，能准确反映文件内容或职责
- 禁止使用泛化/无意义命名，如 `blank`、`temp`、`test1`、`utils` 单独作为文件名
- 目录名使用单数形式（`component` 不是 `components`，`style` 不是 `styles`）
- 常量文件：以内容类别命名（如 `colorDefaults.ts`），不用 `constants`、`tokens` 等泛化词

## 三端适配

开发功能时必须同时适配三端，使用项目已有断点变量：

- **PC 端**（> 1024px）：默认布局
- **平板端**（768px ~ 1024px）：`@media (max-width: $doc-bp-tablet)`
- **手机端**（< 768px）：`@media (max-width: $doc-bp-mobile)`

确保交互元素在触屏设备上可用（点击区域不小于 32×32px），布局在窄屏下不溢出。

---

## 组件一站式创建规范

> **适用前提**：新增或修改 `packages/comp/src/component/` 下组件、同步 Demo 文档、更新 `packages/comp/src/doc/CompDocRegistrar.ts` 路由时，必须完整遵循本节。

- **新增**顶层对外组件：流程为 **先确认分类 → 再创建代码 → 最后同步 Demo + 导出**
- **修改**已有顶层组件（修 bug、调样式/逻辑、API 小改）：**跳过分类询问**，仍须遵守代码规范、三端适配；若 Props/Events 变更则同步 Demo

### 一、创建前：确认组件分类（仅新增顶层组件）

参照上文「组件分类（全局）」表格。**必须先向开发者确认**组件属于哪一类；仅在需求已明确指定分类时方可直接执行。表格未覆盖的目录须先按全局硬约束完成新增目录确认与规则同步。

| 分类 | 目录 | 类型文件 | 命名前缀示例 |
|------|------|----------|-------------|
| **CRUD** | `packages/comp/src/component/crud/` | `packages/comp/src/type/crud.ts` | `BaseTable`、`BaseSearch` |
| **基础** | `packages/comp/src/component/basic/` | `packages/comp/src/type/basic.ts` | `Tag`、`Dot`、`ImageCarousel` |
| **交互** | `packages/comp/src/component/interaction/` | `packages/comp/src/type/interaction.ts` | `SplitPane`、`DragSortList` |

若表格所列目录均不适合，按上文「组件分类（全局）」硬约束询问开发者；确认新增目录后须回写全局分类表、本节分类表，并按全局硬约束完成类型文件与 Demo 菜单同步。

### 二、目录与文件结构

#### CRUD 组件

```
packages/comp/src/component/crud/
└── base-xxx/                    # kebab-case，语义化
    ├── BaseXxx.vue              # PascalCase，与导出名一致
    └── ...                      # 子模块按需：modes/ utils/ theme/
```

- 可被多个 CRUD 组件复用的子组件：同级新建目录，如 `base-search-field/BaseSearchField.vue`
- 仅某一组件内部使用：放在父组件子目录，如 `base-table/modes/CellSwitch.vue`；能否抽离到 `basic/` 见全局硬约束（可独立使用则询问，不可则内部不导出）

#### 基础组件

```
packages/comp/src/component/basic/
└── tag/                         # kebab-case
    └── Tag.vue                  # PascalCase
```

#### 交互组件

```
packages/comp/src/component/interaction/
└── split-pane/                  # kebab-case
    └── SplitPane.vue            # PascalCase
```

#### 命名对照

| 项 | 规范 | 示例 |
|----|------|------|
| 文件夹 | kebab-case，带业务前缀 | `base-search-field`、`image-carousel` |
| Vue 文件 | PascalCase = 组件名 | `BaseSearchField.vue` |
| 类型文件 | 统一放 `packages/comp/src/type/` | `basic.ts`、`crud.ts`、`interaction.ts` |
| 组合式函数 | camelCase + `use` 前缀 | `useBaseTableSelection.ts` |
| 工具函数 | camelCase | `column.ts`、`canvasDraw.ts` |
| Demo 页 | `Demo{ComponentName}.vue` | `DemoBaseSearchField.vue` |
| defineOptions name | 与导出名一致 | `{ name: "BaseSearchField" }` |

#### 禁止

- 泛化文件名：`utils.ts`、`temp.vue`、`blank.vue`
- 硬编码颜色（组件内用 `packages/core/src/style/variables.scss` 的 `$lib-*`）
- 未经确认自行新增 `component/` 子目录或 npm 依赖

### 三、代码模板与实现约束

编写或修改组件代码时，须同时遵守：**路径引用规范**、**代码风格**、**三端适配**、**组件复用原则**（禁止重复实现已有 `CodeBlock` / `WidgetTabs` 等）。

```vue
<script setup lang="ts">
import type { XxxProps, XxxEmits } from "@/type/crud"; // 或 @/type/basic、@/type/interaction

defineOptions({ name: "Xxx" });

const props = withDefaults(defineProps<XxxProps>(), { /* 默认值 */ });
const emit = defineEmits<XxxEmits>();
</script>
```

- Props / Emits 类型声明在 `packages/comp/src/type/basic.ts`、`crud.ts` 或 `interaction.ts`，通过 `@/type/` 引入
- 注释优先中文（公共 API 的 JSDoc 除外）

### 四、导出与注册（对外发布的顶层组件）

1. **`packages/comp/src/component/index.ts`** — 按分类注释分组导出
2. **`packages/comp/src/type/{category}.ts`** — 新增类型声明
3. **`packages/comp/src/index.ts`** — 具名导出组件 + `export type { XxxProps, XxxEmits }`
4. **`packages/comp/src/index.ts` 的 `components` 数组** — 加入 `install()` 注册
5. 大业务组件的内部实现（如 `base-table/modes/*`）**不在此列表注册**
6. `doc/架构设计.md` — **仅当开发者明确要求更新文档时**维护

### 五、创建后检查清单

- [ ] 分类目录正确（`basic/` / `crud/` / `interaction/` 或已确认的特殊路径）
- [ ] 命名符合上表
- [ ] 已从 `packages/comp/src/index.ts` 导出
- [ ] 已创建 Demo 页并注册菜单（见下文第六节）
- [ ] 三端断点适配（`$doc-bp-tablet` / `$doc-bp-mobile`）
- [ ] 未引入无 Demo 的对外组件

### 六、Demo 文档自动生成规范

**组件代码完成后，必须同步创建 Demo 文档页。**

#### 6.1 文件与导航

- 文档页：`packages/comp/src/doc/demo/{component-name}/Demo{ComponentName}.vue`
- 在 `packages/comp/src/doc/CompDocRegistrar.ts` 中：
  - `docLoaders` 增加路由 key → Demo 组件
  - `docRouteMeta` 增加菜单项（CRUD / 基础 / 视觉 / 交互 分组与组件分类一致）
- 路由 key 用 kebab-case，如 `base-search-field`

#### 6.2 页面结构（固定顺序）

```vue
<!-- 1. 页头 -->
<div class="doc-content__header"><h2>组件名</h2><p>一句话说明</p></div>

<!-- 2. 演示区：DemoWidgetTabs（预览 / 代码切换，BaseTable Demo 除外） -->
<DemoWidgetTabs :code="demoCodes.xxx">
  <!-- 预览内容 -->
</DemoWidgetTabs>

<!-- 3. API 区 api-section + ApiTable -->
<div class="api-section">
  <h3 class="api-section__title">组件名 Props</h3>
  <ApiTable type="props" :rows="api.props" />
</div>

<!-- 4. 特殊说明（有 api.notes 时） -->
<div v-if="api.notes?.length" class="api-section">
  <h3 class="api-section__title">特殊说明</h3>
  <ul class="api-notes-list">
    <li v-for="(note, idx) in api.notes" :key="idx">{{ note }}</li>
  </ul>
</div>
```

- 样式：`@use "../style/variables" as *;` + `@use "../style/demo";`
- 禁止硬编码文档色值；类型标签色由 `demo.scss` 的 `.api-type` 跟随 theme 主色
- 补充说明统一标题为 **特殊说明**，列表使用 `api-notes-list`

**演示区布局（DemoWidgetTabs，BaseTable 多模式 Demo 除外）**

| 文件 | 职责 |
|------|------|
| `DemoWidgetTabs.vue` | 演示卡片容器：EP Tabs + 预览/代码 + 复制 |
| `demoCodes.ts` | 各组件示例代码字符串，**必须含 script 数据定义 + template** |
| `apiTypeHints.ts` | opaque 类型 `?` 提示注册 |
| `demoCodeHighlight.ts` | highlight.js 封装 |

#### 6.2.1 demoCodes 与 apiTypeHints（新增组件必做）

**`demoCodes.ts`**

- 每个新组件必须在 `demoCodes.ts` 新增 camelCase 条目
- 条目格式为完整 SFC 片段：`<script setup lang="ts">` + `<template>`
- **禁止**只写 `<template>` 片段而缺少 script 中的数据定义

**`apiTypeHints.ts`**

- Props / Events 中出现 **opaque 类型名**时，必须在 `packages/comp/src/doc/type/apiTypeHints.ts` 注册
- 字面量联合类型写完整即可，无需注册

#### 6.3 Tab 与容器

- 必须使用 `DemoWidgetTabs.vue`，内部为 Element Plus **`el-tabs` + `type="border-card"`**
- **禁止**自定义 Tab 按钮、禁止裸 `widget-card` 包一层
- **唯一例外**：`DemoBaseTable.vue` 多模式表格 Demo 不使用 DemoWidgetTabs

**预览 Tab 三段结构**

1. **顶部**：EP Tab 标签栏（预览 / 代码）
2. **中部**：组件预览；表格 / BaseCrud 等全宽贴边时加 **`flush`**
3. **底部**（可选）：`#footer` 插槽放「最近事件」

**代码 Tab**

- 高亮：`highlight.js`（`demoCodeHighlight.ts`），语言 `xml`
- **hljs token 样式必须写在 `DemoWidgetTabs.vue` 的非 scoped `<style>` 块**
- 复制按钮：代码块右上角绝对定位浮层

**事件反馈（最近事件）**

- 有事件监听时，统一 `lastEvent` + `#footer` 插槽 + `.widget-hint`
- **禁止**放在中部预览区

#### 6.4 API 表格

- **必须**使用 `ApiTable.vue`，禁止手写 API 表格
- Props / Events / Slots 定义在 `<script setup>` 的 `ComponentApi` 或 `ApiRow[]`

#### 6.5 类型列写法

| 场景 | 写法 | 是否显示 `?` |
|------|------|-------------|
| 字面量枚举 | `'"success" \| "warning" \| "info" \| "danger"'` | 否 |
| 字面量联合 | `'"left" \| "center" \| "right"'` | 否 |
| 基础类型 | `string` / `boolean` / `number` | 否 |
| 仅类型名、看不出具体值 | `BaseTableMode` / `DragSortItem[]` | 是（自动） |

#### 6.6 Demo 检查清单

- [ ] `Demo{ComponentName}.vue` 已创建
- [ ] `CompDocRegistrar` 路由与分组已注册
- [ ] 演示区使用 `DemoWidgetTabs`（BaseTable 除外），`demoCodes.ts` 已注册对应条目
- [ ] 有事件反馈时用 `#footer` + `widget-hint`
- [ ] 使用 `ApiTable` 渲染 API
- [ ] 新 opaque 类型已注册 `apiTypeHints.ts`
- [ ] 演示区三端适配

---

## Cursor 规则维护

- 规则唯一源文件：`.cursor/rules/project-conventions.mdc`（分类目录列表写在本文件「组件分类（全局）」表格中，**无单独机器可读配置文件**）
- 修改该规则文件后，须同步更新 `doc/组件规则流程图.md`
- `doc/架构设计.md`、`CHANGELOG.md` 仅在开发者明确要求时更新，不随组件/规则改动自动写入

---

## CHANGELOG 维护

当用户要求记录变更时，按以下格式更新 `CHANGELOG.md`。三级层级结构：

- **一级**：模块分类（`### 组件模块`、`### 配置模块`、`### 工程化` 等）
- **二级**：操作类型（`#### 新增组件`、`#### 组件调整`、`#### 新增配置`、`#### 配置调整` 等）
- **三级**：具体条目，同一对象的多项改动合并为一条

如果某个层级没有内容则不展示。

条目精简规则：
- 只记录"做了什么"，不记录"怎么实现的"
- 不写括号内的实现细节、技术手段、具体参数
- 同类变更合并为一句话，避免子列表展开
- 同一版本内新增的内容不记录中途重命名/重构过程，直接以最终形态记录为"新增"

版本规则：
- 新功能 minor+1，纯修复 patch+1
- 如果当天已有版本条目，合并到同一版本中
- 连续多天的改动合并为同一版本，日期标注为范围格式：`vX.Y.Z（YYYY-MM-DD ~ YYYY-MM-DD）`
- 新增或更新版本条目时，必须先执行 `git log` 查看提交记录，根据该版本首次和最后一次提交的时间确定日期范围，不得凭假设填写日期

## Git 提交规范

提交人固定为 `jsh <1051131737@qq.com>`。提交信息使用中文，不得出现 AI / Cursor / 自动生成等字样。

格式：

```
type(scope): 一句话概括

模块分类A
- 具体变更条目

模块分类B
- 具体变更条目
```

**type** 取值：`feat` 新功能、`fix` 修复、`refactor` 重构、`style` 格式/样式、`docs` 文档、`chore` 工程化

**scope** 取值：模块名（如 `config`、`table`、`search`、`demo`、`build`），可省略

**body** 按模块分类组织（组件模块、配置模块、工程化等），每条以 `- ` 开头，同类合并。

## 架构文档维护

维护 CHANGELOG 时，如涉及以下变更须同步更新 `doc/架构设计.md`：

- 新增或删除组件 → 更新「目录结构」和「组件设计模式」章节
- BaseTable 模式变化（新增/移除/重构） → 更新「核心架构：BaseTable 五模式」章节
- 主题系统变化 → 更新「主题系统」章节
- 构建配置或分发方式变化 → 更新「构建与分发」章节
- 技术栈升级（如 Vue/Vite 大版本） → 更新「技术栈」表格
- 新版本发布 → 在「版本演进概览」表格追加条目
