---
description: 项目级通用约定，每次对话自动生效
alwaysApply: true
---

# 项目约定

# Cursor Feedback 规则

1. 在任何流程、任务、对话进行时，无论是询问、回复、或完成阶段性任务，皆必须调用 MCP cursor-feedback。
2. 每当收到用户反馈，必须再次调用 MCP cursor-feedback，并根据反馈内容调整行为。
3. 完成任务前，必须使用 MCP cursor-feedback 工具向用户询问反馈。
4. 所有需要展示给用户的内容必须全部通过 cursor-feedback 的 summary 参数传递。

## 语言

始终使用简体中文回复。代码注释也优先使用中文，除非是公共 API 的 JSDoc。

## 目录结构规范

`src/` 下目录名使用单数形式，各职责分离：

```
src/
  component/           ← Vue 组件源码
    basic/             ← 基础组件（Tag、Dot、CodeBlock、WidgetTabs 等）
    crud/              ← CRUD 组件（BaseTable、BaseSearch 等）
  config/              ← 配置注入系统
  style/               ← SCSS 变量 + TS 颜色默认值
  type/                ← 类型声明（所有组件类型的唯一源头）
    basic.ts           ← 基础组件类型
    crud.ts            ← CRUD 组件类型
    index.ts           ← 统一 re-export
  util/                ← 工具函数
  index.ts             ← 库入口
```

`examples/` 下按职责分离：

```
examples/
  App.vue              ← 演示站入口
  style/               ← 演示站 SCSS（variables、demo、el-overrides 等）
  type/                ← 演示站类型（ApiRow、apiTypeHints 等）
  demo/                ← 组件 Demo 页
    base-table/        ← 每个组件独立子目录
    base-search/
    config-provider/
    ...
    ApiTable.vue       ← 共享 Demo 辅助组件
    DemoCodeBlock.vue  ← 代码展示 + 复制（通用）
    DemoWidgetTabs.vue ← 预览/代码 Tab 切换
    demoCodes.ts
    demoCodeHighlight.ts
```

## 路径引用规范

- `src/` 内的文件，跨目录引用必须使用 `@/` alias（`@` 指向 `src/`）
- 同目录内用 `./` 相对引用
- 禁止出现三层及以上的相对路径（`../../../`）
- SCSS `@use` 同样支持 `@/style/variables`
- `examples/` 下的 Demo 文件引用 `src/` 内容时使用 `@/` 或 `comp-vue-lib` 别名
- 唯一例外：`node_modules` 运行时资源引用和项目根文件（如 `CHANGELOG.md`）

## 代码风格

- Vue 组件使用 `<script setup lang="ts">` + Composition API
- 组件文件名：PascalCase（如 `ChangelogPanel.vue`）
- 组合式函数文件名：camelCase，`use` 前缀（如 `useCanvasCheckboxHover.ts`）
- 工具函数文件名：camelCase（如 `canvasDraw.ts`）
- SCSS 变量统一定义在 `src/style/variables.scss`（组件库公共变量源头），Demo 页通过 `@use` 继承
- 主题 token 统一定义在 `src/component/crud/base-table/theme/tableSurface.ts`
- 类型声明统一放在 `src/type/`，组件目录内不再保留 `types.ts`
- 不要自行添加 npm 依赖，除非明确要求

## 组件复用原则

- 相同功能/UI 不允许重复实现，必须提取为通用组件复用
- 代码展示 + 复制功能统一使用组件库的 `CodeBlock` 组件，不要自行实现复制逻辑
- 预览/代码切换容器统一使用组件库的 `WidgetTabs` 组件（默认 `flush: true`）
- `examples/demo/DemoCodeBlock.vue` 和 `DemoWidgetTabs.vue` 是 Demo 站的薄包装层
- 新增 Demo 辅助组件前，先检查 `src/component/basic/` 下是否已有可复用的组件

## Demo 文档规范

- 每个组件的 Demo 页统一使用 `DemoWidgetTabs` 包裹，展示「预览」和「代码」两个 Tab
- 「代码」Tab 的内容必须是预览区的真实源代码，不允许放通用示例或占位代码
- Demo 页结构：标题描述 → DemoWidgetTabs（可多个场景） → API 参数表
- 工具函数 Demo 统一使用 `CodeBlock` 展示代码，同样保持一致的复制功能

## npm 包导出规范

组件和工具函数使用不同的导入路径：

```ts
// 组件（主入口）
import { BaseTable, CodeBlock } from "comp-vue-lib";

// 工具函数（/util 子路径）
import { rgbaToHex, debounce } from "comp-vue-lib/util";
import { rgbaToHex } from "comp-vue-lib/util/color";

// 类型（/type 子路径）
import type { TagProps, BaseTableColumn } from "comp-vue-lib/type";

// 样式
import "comp-vue-lib/style.css";
```

- `src/index.ts` 只导出组件和配置，不导出工具函数和类型
- 工具函数通过 `src/util/index.ts` 统一导出
- 类型通过 `src/type/index.ts` 统一导出
- `package.json` 的 `exports` 字段维护子路径映射
- 构建使用 `vite.lib.config.ts`（多入口），确保每个子路径有独立产物

## 文件命名

- 新增或修改文件时，文件名必须语义化，能准确反映文件内容或职责
- 禁止使用泛化/无意义命名，如 `blank`、`temp`、`test1`、`utils` 单独作为文件名
- 目录名使用单数形式（`component` 不是 `components`，`style` 不是 `styles`）
- 常量文件：以内容类别命名（如 `colorDefaults.ts`），不用 `constants`、`tokens` 等泛化词

## 三端适配

开发功能时必须同时适配三端，使用项目已有断点变量：

- **PC 端**（> 1024px）：默认布局
- **平板端**（768px ~ 1024px）：`@media (max-width: $doc-bp-tablet)`
- **手机端**（< 768px）：`@media (max-width: $doc-bp-mobile)`

确保交互元素在触屏设备上可用（点击区域不小于 32×32px），布局在窄屏下不溢出。

## CHANGELOG 维护

当用户要求记录变更时，按以下格式更新 `CHANGELOG.md`。三级层级结构：

- **一级**：模块分类（`### 组件模块`、`### 配置模块`、`### 工程化` 等）
- **二级**：操作类型（`#### 新增组件`、`#### 组件调整`、`#### 新增配置`、`#### 配置调整` 等）
- **三级**：具体条目，同一对象的多项改动合并为一条

如果某个层级没有内容则不展示。示例：

```markdown
## vX.Y.Z（YYYY-MM-DD 或 YYYY-MM-DD ~ YYYY-MM-DD）

### 组件模块

#### 新增组件
- **ChangelogPanel**：变更记录时间线面板，支持版本折叠展开

#### 组件调整
- **BaseTable**：统一五种模式列宽计算；新增 hover 交互
- **SearchBar**：修复清空按钮不触发事件的问题

### 配置模块

#### 新增配置
- **Cursor 规则**：新增项目约定规则文件
```

条目精简规则：
- 只记录"做了什么"，不记录"怎么实现的"
- 不写括号内的实现细节、技术手段、具体参数
- 同类变更合并为一句话，避免子列表展开
- 同一版本内新增的内容不记录中途重命名/重构过程，直接以最终形态记录为"新增"

版本规则：
- 新功能 minor+1，纯修复 patch+1
- 如果当天已有版本条目，合并到同一版本中
- 连续多天的改动合并为同一版本，日期标注为范围格式：`vX.Y.Z（YYYY-MM-DD ~ YYYY-MM-DD）`
- 新增或更新版本条目时，必须先执行 `git log` 查看提交记录，根据该版本首次和最后一次提交的时间确定日期范围，不得凭假设填写日期

## Git 提交规范

提交人固定为 `jsh <1051131737@qq.com>`。提交信息使用中文，不得出现 AI / Cursor / 自动生成等字样。

格式：

```
type(scope): 一句话概括

模块分类A
- 具体变更条目

模块分类B
- 具体变更条目
```

**type** 取值：`feat` 新功能、`fix` 修复、`refactor` 重构、`style` 格式/样式、`docs` 文档、`chore` 工程化

**scope** 取值：模块名（如 `config`、`table`、`search`、`demo`、`build`），可省略

**body** 按模块分类组织（组件模块、配置模块、工程化等），每条以 `- ` 开头，同类合并。遵循 CHANGELOG 精简规则——只记录做了什么，不记录怎么实现的。

示例：

```
feat(config): 配置系统重构为保存模式，全局主题适配

配置模块
- 移除 updateConfig/resetConfig，新增 saveConfig
- 配置页改为手动保存，移除 watch 自动生效

组件调整
- 侧边栏菜单跟随主题变化
- 配置页新增移动端适配

工程化
- Stylelint 新增 Vue 伪类白名单
- 代码格式化
```

## 架构文档维护

维护 CHANGELOG 时，如涉及以下变更须同步更新 `doc/架构设计.md`：

- 新增或删除组件 → 更新「目录结构」和「组件设计模式」章节
- BaseTable 模式变化（新增/移除/重构） → 更新「核心架构：BaseTable 五模式」章节
- 主题系统变化 → 更新「主题系统」章节
- 构建配置或分发方式变化 → 更新「构建与分发」章节
- 技术栈升级（如 Vue/Vite 大版本） → 更新「技术栈」表格
- 新版本发布 → 在「版本演进概览」表格追加条目
